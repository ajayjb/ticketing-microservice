apiVersion: v1
kind: ConfigMap
metadata:
  name: ticketing-tickets-config
data:
  PORT: "6000"
  LOG_LEVEL: "info"
  LOG_DIR: "logs"
  DB_NAME: "tickets"
  DB_HOST: "mongo-tickets-srv"
  DB_PORT: "27017"
  DB_MIN_POOL_SIZE: "5"
  DB_MAX_POOL_SIZE: "10"
  NATS_CLUSTER_ID: "tickets"
  NATS_URL: "nats://nats-srv:4222"

---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: ticketing-tickets-depl
spec:
  replicas: 1
  selector:
    matchLabels:
      app: ticketing-tickets
  template:
    metadata:
      labels:
        app: ticketing-tickets
    spec:
      containers:
        - name: ticketing-tickets
          image: ajayjb/ticketing-tickets:latest
          envFrom:
            - configMapRef:
                name: ticketing-tickets-config
          env:
            - name: ENVIRONMENT
              valueFrom:
                secretKeyRef:
                  name: environment-secret
                  key: ENVIRONMENT
            - name: JWT_KEY
              valueFrom:
                secretKeyRef:
                  name: environment-secret
                  key: JWT_KEY
            - name: NATS_CLIENT_ID
              valueFrom:
                fieldRef:
                  fieldPath: metadata.name
            - name: DB_USER
              valueFrom:
                secretKeyRef:
                  name: ticketing-tickets-secret
                  key: DB_USER
            - name: DB_USER_PWD
              valueFrom:
                secretKeyRef:
                  name: ticketing-tickets-secret
                  key: DB_USER_PWD

---
apiVersion: v1
kind: Service
metadata:
  name: ticketing-tickets-srv
spec:
  type: ClusterIP
  selector:
    app: ticketing-tickets
  ports:
    - name: ticketing-tickets-srv-port
      protocol: TCP
      port: 6000
      targetPort: 6000
